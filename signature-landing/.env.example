# URL de la landing page para tests E2E
LANDING_URL=http://localhost:3001

# URL del backend API
API_URL=http://localhost:3000

# Token de autenticación para el API (si es necesario)
API_AUTH_TOKEN=Bearer your-test-token-here

# Modo headless para Playwright (true/false)
HEADLESS=true

# Delay entre acciones en milisegundos (útil para debug)
SLOWMO=0

# Configuración SMTP para envío de emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-app-password
ADMIN_EMAIL=admin@firmatic.com
